# Database
DB_HOST=localhost
DB_NAME=repair_crm
DB_USER=
DB_PASS=

# Telegram Bot (optional)
TG_BOT_TOKEN=

# AI Integration (optional)
AI_API_KEY=
AI_MODEL=google/gemini-2.0-flash-001
AI_PROVIDER=openrouter

# iFreeiCloud IMEI API (optional)
IFREEICLOUD_API_KEY=
IFREEICLOUD_SERVICE_ID=205

# GitHub self-update (required for in-app update of a PRIVATE repo)
# Fine-grained PAT with "Contents: Read-only" on Augi23/Fix-CRM. Used only for git fetch/pull,
# never written to .git/config. Leave empty for public repos / SSH remotes.
GITHUB_TOKEN=
