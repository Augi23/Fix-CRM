; Limity nahrávání (fotky/videa k zakázkám) — PHP-FPM čte per adresář,
; změna se projeví do ~5 minut (user_ini cache). Systémový default 2M nestačil.
upload_max_filesize = 64M
post_max_size = 80M
max_file_uploads = 30
